// #{export} Print ::= (n: i64) -> () {
//   printf ::= builtin.foreign("printf", ([*]char, i64) -> i32)
//   printf("%lld".data, n)
// }
// 
// #{export} Print ::= (n: u64) -> () {
//   printf ::= builtin.foreign("printf", ([*]char, u64) -> i32)
//   printf("%llu".data, n)
// }
// 
#{export} Print ::= (s: [/]char) -> () {
  printf ::= builtin.foreign("printf", ([*]char, u64, [*]char) -> i32)
  printf("%*s\n".data, s.length, s.data)
}

Print("Hello, world!")
