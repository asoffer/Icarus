-- ::= import "examples/lib/core.ic"

factorial ::= (n: int) -> int {
  if (n < 2) { return 1 }
  return n * (n - 1)'factorial
}

main ::= void -> void {
  n := 0
  while (n <= 10) {
    print n, "! = ", n'factorial, "\n"
    n += 1
  }
}
