--     ::= import "core.ic"
str    ::= import "string.ic"
io     ::= import "io.ic"

a := str.make("this is a string\n")
a'Print
b := str.make("here's another\n")
b'Print

c := str.make(a'length + b'length)
c'append(b)
c'append(a)
c'Print

io.Print(c'at(5)[0])
io.Print(c'view[5])
io.Print(!\n)
