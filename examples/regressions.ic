// This file contains simple snippets that have triggered bugs in the past.

// Test casting from a buffer pointer to a raw pointer. (gh-106)
BufferToRawPointer ::= (data: [*]i64) -> *i64 {
  return &data[0]
}

// Test recursive short-function type checking. (gh-99)
// Foo ::= (x: i64) => x
// Foo ::= (x: i64, y: i64) => Foo(x)

// Test constructing arrays of custom types. (gh-107)
custom ::= struct { x: i64 }
my_data: [3; custom]
