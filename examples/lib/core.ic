if ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= void => false
}

forever ::= scope {
  enter ::= void => true
  exit ::= void => true
}

while ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= void => true
}
