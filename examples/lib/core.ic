if ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= () => false
}

forever ::= scope {
  enter ::= () => true
  exit ::= () => true
}

while ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= () => true
}
