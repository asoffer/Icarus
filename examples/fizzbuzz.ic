-- ::= import "examples/lib/core.ic"

main ::= () -> () {
  i := 0
  while (i < 100) {
    if (i % 15 == 0) {
      print "FizzBuzz\n"
    } else { if (i % 3 == 0) {
      print "Fizz\n"
    } else { if (i % 5 == 0) {
      print "Buzz\n"
    } else {
      print i, "\n"
    } } }
    i += 1
  }
  print "\n"
}
