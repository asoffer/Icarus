vec ::= import "examples/lib/vec.ic"
-- ::= import "core.ic"
io ::= import "io.ic"

v := vec.make(int64)
i := 0
while (i < 30) do {
  vec.append(int64, i, &v)
  vec.dump(v)
  io.Print("\n")
  i += 1
}

io.Print(vec.remove(int64, &v))
io.Print("\n")

vec.dump(v)
io.Print("\n")

