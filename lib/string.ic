string := struct {
  _chars: [--; char]
  _length: uint
}

__print__ := (s: string) -> void {
  for c in s._chars[0u .. s._length - 1u] { print c }
}

c_str := (s: &string) -> &char { return &s._chars[0] }

size := (s: &string) -> uint { return s._length }
