string := struct {
  chars: [100; char] // TODO replace 100 with --
  length: uint
}

__print__ := (s: string) -> void {
  for i in 0u .. s.length - 1u {
    print s.chars[i]
  }
}

size := (s: string) -> uint {
  return s.length
}
