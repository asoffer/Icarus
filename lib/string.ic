string := struct {
  chars: [--; char]
  length: uint
}

__print__ := (s: string) -> void {
  for c in s.chars[0u .. s.length - 1u] {
    print c
  }
}

size := (s: string) -> uint {
  return s.length
}
