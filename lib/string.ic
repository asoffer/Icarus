string := struct {
  chars: [-; char]
  length: uint
}

__print__ := (s: string) -> void {
  i := 0u
  while i < s.length {
    print s.chars[i]
    i += 1u
  }

//  for c in s.chars[ .. s.length - 1] {
//    print c
//  }
}

size := (s: string) -> uint {
  return s.length
}
