import "lib/prelude.ic"

u := (n: int) -> uint { return n :> uint }

main := void -> void {
  print "hello\n"
  t := "world\n"
  print t
  print "t has length ", t'size, `\n
}

size := (s: string) -> uint {
  for i in 0 .. {
    if s[i] == 0'u'ascii { return i }
  }
}
