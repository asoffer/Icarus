main := void -> void {
  n := 1
  while true {
    f: foo
    n *= 2
    if n > 1000 {
      g: foo
      break
    }
    print "*"
  }
  
  print " -> "
  print n
  print "!\n"
}

// To see that the dealloc happens we need a struct that requires a deallocation.
foo := struct {
  x: [3; real]
}
