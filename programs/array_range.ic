main := void -> void {
  a: [10, 10; int]
  for i in 0 .. 9 {
    for j in 0 .. 9 {
      a[i][j] = (i * j) % 10
    }
  }

  for row in a {
    for entry in row {
      print entry, ", "
    }
    print "\n"
  }

  print "\n\n\n"

  for i in (0:>uint) .. a.size - (3:>uint) {
    for subrow in a[i .. i + (2:>uint)] {
      for entry in subrow[i .. i + (2:>uint)] {
        print entry, ", "
      }
      print "\n"
    }
    print "\n\n"
  }
}
