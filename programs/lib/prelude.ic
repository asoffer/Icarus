if := scope {
  enter := (cond: bool) -> bool { return cond }
  exit := void -> bool { return false }
}

while := scope {
  enter := (cond: bool) -> bool { return cond }
  exit := void -> bool { return true }
}

forever := scope {
  enter := void -> bool { return true }
  exit := void -> bool { return true }
}
