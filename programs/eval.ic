hi := void -> void {
  print  "Hi\n"
} #const

fibonacci := (n: int) -> int {
  return case {
    n == 0 => 0
    n == 1 => 1
    else   => fibonacci(n - 1) + fibonacci(n - 2)
  }
} #const

fib_of_20 := void -> int { return fibonacci(20) } #const
main := void -> void {

  $(hi())
  $print fib_of_20(), `\n
  print $(fib_of_20())
}
