// Intentionally missing return statement because we don't treat it
// appropriately yet.

factorial : int -> int
factorial = (n : int) -> int {
   return case {
    n < 2  =>  1
    else   =>  n * factorial(n - 1)
  }
}

factorial(factorial(3))
