square := (x : real) -> real {
  return x * x
}

wat := (x : real) -> real {
  y : real
  y = 3.0
  y += x
  y = y / 2.0
  return y * y
}

foo := (n : int) -> int {
  n *= n
  n += n
  return n + 3
}
