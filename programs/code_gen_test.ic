square : real -> real = (x : real) -> real {
  return x * x
}

wat : real -> real = (x : real) -> real {
  y : real
  y = 3.0
  y += x
  y = y / 2.0
  return y * y
}

foo : int -> int = (m : int) -> int {
  n := m
  n *= n
  n += n
  return n + 3
}

main := void -> void {
  return
}
