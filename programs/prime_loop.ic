main := void -> void {
  num_primes := 10
  m := 2
  
  while num_primes > 0 {
    if m'is_prime {
      print m, ", "
      num_primes -= 1
    }
    m += 1
  }
}

is_prime := (n: int) -> bool {
  k := 2
  b := true

  while k * k <= n {
    b &= n % k != 0
    k += 1
  }

  return b
}
