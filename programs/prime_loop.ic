import "prelude.ic"

main := void -> void {
  i := 2
  forever {
    if i 'is_prime { print i, ", " }
    i += 1
  }
}

is_prime := (n: int) -> bool {
  i := 2
  while i * i <= n {
    if n % i == 0 { return false }
    i += 1
  }
  return true
}
