// Segfaults because strings are not yet incorporated
water_state := (temp : real) -> string {
   return case {
    temp < 32         =>  "solid"
    32 <= temp < 212  =>  "liquid"
    else              =>  "gas"
  }
}
