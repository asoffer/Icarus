import "fibonacci.ic"

Any := (T: type) -> bool {
  print fib(10), ' '
  return true
}

foo := (val: Any`T) -> void {
  print val
}

main := void -> void {
  print '-', ' ', fib(10), ' ', '-', '\n'
  foo(3)
  foo(3.0)
  foo('x')
}
