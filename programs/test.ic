g := (n: int) -> int {
  return n + 1
}

f := (n: int) -> int {
  return g(g(n) * g(2))
}

Any := (T: type) -> bool {
  print '>', ' ', f(3), '\n'
  return true
}

foo := (val: Any`T) -> void {
  print val
}

main := void -> void {
  foo(3)
  foo(3.0)
  foo('x')
}
