Any := (T: type) -> bool {
  TT := case {
    (T.bytes <=  4u) => int
    else             => real
  }
  val: TT
  print (T.bytes <=  4u), ' ', TT, ' ', '-', '>', ' ', val, '\n'

  return true
}

foo := (val: Any`T) -> void {
  print val
}

main := void -> void {
  foo(3)
  foo(3.0)
  foo('x')
}

