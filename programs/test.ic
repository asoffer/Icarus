factorial := (n: int) -> int {
  return case {
    n < 2 => 1
    else  => n * factorial(n - 1)
  }
}

f := (val: Some`T) -> void {
  print val
}

Some := (T: type) -> bool {
 print factorial(4)
  return true
}

main := void -> void {
  f(3)
}
