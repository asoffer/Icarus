Int := wrapper(int)

main := void -> void {
  x: Int
  x.value = 3
  y: wrapper(char)
  y.value = ascii(65u)
  print y.value
  y.value = '\n'
  print y.value
  print wrapper(char) == wrapper(char)
  print y.value
  print wrapper(char) == wrapper(int)
  print y.value
  print wrapper(int) == Int
  print y.value
}

wrapper := struct (T: type) {
  value: T
}
