require "lib/prelude.ic"
require "lib/file.ic"
01234
u := (n: int) -> uint { return n :> uint }

main := void -> void { 
  f: File
  f._handle = open("programs/cheating_quine.ic", FileMode.Read)

  c := (&f)'Next
  while c != 0'u'ascii {
    print c
    c = (&f)'Next
  }
}
