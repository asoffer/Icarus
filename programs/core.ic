if ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= void => false
}

forever ::= scope {
  enter ::= (cond: bool) => true
  exit ::= void => true
}

while ::= scope {
  enter ::= (cond: bool) => cond
  exit ::= void => true
}
