Any := (T: type) -> bool {
  return true
}

alloc := (ptr: &&Any`T) -> void {
  malloc: uint -> &char #cstdlib
  @ptr = malloc(T.bytes) :> &T
}

main := void -> void {
  x: &int
  alloc(&x)
  free x
}
