require "math.ic"

main := void -> void {
  sin2 := (x: real) -> real {
    return sin(x) * sin(x)
  }

  cos2 := (x: real) -> real {
    return cos(x) * cos(x)
  }

  for n in 0 .. 6283 {
    x := (n :> real) / 1000.0
    print sin2(x) + cos2(x)
    print `n
  }

  log_of_exp := log * exp

  print `n
}
